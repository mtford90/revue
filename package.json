{
	"name": "revue-monorepo",
	"version": "0.0.0",
	"private": true,
	"description": "Narrative code review in your terminal — Stage's chapters with a Revue-owned TUI renderer.",
	"type": "module",
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"revue": "bun run packages/tui/src/main.tsx",
		"check": "bun run packages/tui/src/main.tsx show examples/sample-run --check",
		"test": "bun test",
		"typecheck": "tsc --noEmit -p packages/types/tsconfig.json && tsc --noEmit -p packages/diff-model/tsconfig.json && tsc --noEmit -p packages/diff-renderer/tsconfig.json && tsc --noEmit -p packages/prep/tsconfig.json && tsc --noEmit -p packages/markdown-export/tsconfig.json && tsc --noEmit -p packages/tui/tsconfig.json",
		"lint": "biome check ."
	},
	"devDependencies": {
		"@biomejs/biome": "^2.0.0",
		"@types/bun": "^1.3.0",
		"typescript": "^5.7.0"
	},
	"engines": {
		"bun": ">=1.3.0"
	}
}
