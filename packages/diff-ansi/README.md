# `@revue/diff-ansi`

A deterministic ANSI presentation adapter over `@revue/diff` and `@revue/theme`, used by the
standalone Revuediff product. It has no terminal runtime, filesystem, process, Git, React, or
OpenTUI dependency. Callers provide one parsed file, explicit layout and width, and a resolved
theme.
